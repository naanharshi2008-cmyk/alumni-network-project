/** @type {import('next').NextConfig} */
const nextConfig = {
  // Hide the Next.js dev tools bubble in the bottom-left corner during development.
  devIndicators: false,
};

module.exports = nextConfig;
